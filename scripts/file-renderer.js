/**
 * Fonctions pour afficher différents types de fichiers.
 * Pour l'instant, on gère uniquement le texte.
 * Les images et autres types seront ajoutés plus tard.
 */
function renderText(content) {
  return content
}

function renderImage(content) {
  return `[Image: ${content}]` // À améliorer plus tard
}
